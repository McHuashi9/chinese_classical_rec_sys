---
id: v1.2.3-1
---

感谢您使用文言文推荐系统。该软件旨在方便学生、文言文爱好者阅读文言文。

该软件的开发者还是学生，能力不足，开发时间有限。软件必然存在诸多不足之处。

该软件还在迭代开发阶段，欢迎您深入体验、提供反馈、提出建议或贡献代码。再次感谢每一位用户！

## 版本改动

- **软件内截图**：设置页「数据与反馈」新增「软件内截图」，可切换到任意界面后截取当前画面并保存到应用数据目录；默认只保存，SnackBar 提供打开截图目录 / 分享截图 / 附带反馈；反馈弹窗可显示截图缩略图并可移除，复制/邮件正文会附带截图路径。
- **Windows 安装包打包加固**：`chinese_core.dll` 不再重复打包到 `lib/`；移除未使用的 `dartjni.dll`；CI 打包前新增关键 DLL 清单与 PE 依赖校验。
- **最近阅读标题去重**：我的页“最近阅读”不再重复显示外层标题与卡片内标题，只保留一个标题。
